# =========================
# 📝 Local notes
# =========================
TODO.md

# =========================
# 🔐 Secrets & env files
# =========================
.env
.env.*

!.env.example

# =========================
# 🗄️ Data directories (very important)
# =========================
data/
*/data/
../../data/
letsencrypt/
*/letsencrypt/

# Postgres backup target (default BACKUP_HOST_DIR). pg_dumpall output includes
# role password hashes.
stacks/postgres/backup/

# Database files (extra safety)
*.db
*.sql
*.sql.gz

!*.dummy.sql
!*.seed.sql

# =========================
# 🏠 Home Assistant config dir
# =========================
# Only the hand-maintained YAML is tracked. Everything HA writes at runtime —
# .storage (auth tokens, entity/device registry), secrets.yaml, logs, deps/,
# custom_components/ — stays local.
stacks/homeassistant/config/*
!stacks/homeassistant/config/configuration.yaml
!stacks/homeassistant/config/automations.yaml
!stacks/homeassistant/config/scripts.yaml
!stacks/homeassistant/config/scenes.yaml

# =========================
# 📦 Docker / runtime junk
# =========================
*.log
logs/
*.pid

# =========================
# 🧠 OS / editor junk
# =========================
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/

# =========================
# 🔑 SSH / keys (just in case)
# =========================
*.pem
*.key
*.crt

# =========================
# 📦 Backup files
# =========================
*.bak
*.backup
*.old

# =========================
# 🧪 Temporary files
# =========================
tmp/
temp/

# =========================
# 🐳 Docker overrides (optional choice)
# =========================
docker-compose.override.yml

# =========================
# 📄 Terraform / infra (future-proofing)
# =========================
*.tfstate
*.tfstate.*
.terraform/

*.tfvars
*.tfvars.json

!*.tfvars.example
!*.tfvars.json.example

# Generated by OpenTofu from vm_addresses, rewritten on every apply.
infra/ansible/inventory.ini

# Holds homelab_domain, kept out of the repo like DOMAIN in the .env files.
infra/ansible/group_vars/*
!infra/ansible/group_vars/*.example
# Generation rules rather than values, so this one is tracked.
!infra/ansible/group_vars/homelab.yml

# Per-VPS overrides. The keys in these are public, but they carry the name and
# email of someone who is not us, and AUDIT already enumerates this homelab's
# weaknesses in public.
infra/ansible/host_vars/*
!infra/ansible/host_vars/*.example

# Where the password lookup keeps the generated secrets it hands to homelab.yml.
infra/ansible/.secrets/

# =========================
# 📁 Node / misc (if you add tools later)
# =========================
node_modules/
dist/
build/
